\begin{code}
{-# OPTIONS --sized-types --without-K --postfix-projections #-}
module Generic.Simple.Syntax.Term (Ty : Set) where

  open import Size
  open import Relation.Nary

  open import Generic.Simple.Syntax Ty
  open import Generic.Simple.Environment Ty using (_∋_)

  infix 20 [_,_]_⊢_
\end{code}

%<*Tm>
\begin{code}
  data [_,_]_⊢_ (d : System) : Size → OpenFam where
    `var  : ∀ {sz} → ∀[ _∋_                             ⇒ [ d , (↑ sz) ]_⊢_ ]
    `con  : ∀ {sz} → ∀[ ⟦ d ⟧s (Scope ([ d , sz ]_⊢_))  ⇒ [ d , (↑ sz) ]_⊢_ ]
\end{code}
%</Tm>
